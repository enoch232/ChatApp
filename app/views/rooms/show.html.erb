<h1>Chat App</h1>
<div>
	<div id = "messages">
		<%= render @messages %>
	</div>
	<div id = "text-input">
		<%= form_for(:room, url: rooms_path, remote: true) do |f| %>

		<%= f.text_field :message %>
		<%= f.submit "Send" %>
		<% end %>

	</div>
</div>
