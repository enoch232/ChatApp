
# Action Cable uses Redis by default to administer connections, channels, and sending/receiving messages over the WebSocket.
production:
  adapter: redis
  url: redis://:test232@pub-redis-19714.us-east-1-1.1.ec2.garantiadata.com:19714

development:
  adapter: async

test:
  adapter: async
